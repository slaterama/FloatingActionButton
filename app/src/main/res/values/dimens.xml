<resources>

	<!-- Default screen margins, per the Android Design guidelines. -->
	<dimen name="activity_horizontal_margin">16dp</dimen>
	<dimen name="activity_vertical_margin">16dp</dimen>

	<!-- Default radius for FloatingActionButton corners. -->
	<dimen name="fab_default_radius">24dp</dimen>
	<!-- Elevation value to use for FloatingActionButtons. Pre-L, it is equal to shadow size. -->
	<dimen name="fab_default_elevation">2dp</dimen>
	<!-- Inset shadow for RoundRectDrawableWithShadow. It is used to avoid gaps between the button
	 and the shadow. -->
	<dimen name="fab_compat_inset_shadow">1dp</dimen>

	<dimen name="fab_size">54dp</dimen>
	<dimen name="fab_radius">27dp</dimen>
	<dimen name="fab_max_elevation">1dp</dimen>
	<dimen name="fab_elevation">0dp</dimen>

</resources>
